{
  "type": "function",
  "name": "object_bboxes",
  "description": "Detect objects in an image and return their bounding boxes and labels that match a user query.",
  "strict": true,
  "parameters": {
    "type": "object",
    "properties": {
      "query": {
        "type": "string",
        "description": "The user query you answered."
      },
      "image_size": {
        "type": "object",
        "description": "The width and height of the analyzed image in pixels.",
        "properties": {
          "width": {
            "type": "integer",
            "description": "Width of the image in pixels."
          },
          "height": {
            "type": "integer",
            "description": "Height of the image in pixels."
          }
        },
        "required": [
          "width",
          "height"
        ],
        "additionalProperties": false
      },
      "boxes": {
        "type": "array",
        "description": "Zero or more bounding boxes that match the query.",
        "items": {
          "type": "object",
          "properties": {
            "label": {
              "type": "string",
              "description": "Short name of the object."
            },
            "confidence": {
              "type": "number",
              "description": "Confidence score for the detection (between 0 and 1).",
              "minimum": 0,
              "maximum": 1
            },
            "xywh": {
              "type": "array",
              "description": "Bounding box formatted as [x, y, width, height] in pixels.",
              "items": {
                "type": "number"
              },
              "minItems": 4,
              "maxItems": 4
            }
          },
          "required": [
            "label",
            "confidence",
            "xywh"
          ],
          "additionalProperties": false
        }
      }
    },
    "required": [
      "query",
      "image_size",
      "boxes"
    ],
    "additionalProperties": false
  }
}